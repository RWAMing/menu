Menu Roulette


~ 메뉴 추천 룰렛 ~

<기본>
- const type = new Map;
- const en = [korean, ....]
- const ko = [한식, ]

- menu.txt에 \n으로 메뉴가 입력되어 있다.
- all_
- 메뉴는 node.js로 menu$.txt 내용을 각 카테고리에 \n으로 쪼개서 배열로 만들어 넣는다.

- 메뉴는 카테고리화 되어있다. (전체선택 / 한식, 중식, 일식, 양식, 후식, 야식, 다이어트..) *배민 참고하기
- 유저가 카테고리를 중복으로 선택할 수 있다.
- 카테고리는 배열이며, 배열 요소는 메뉴이다.

- 배열형 변수 m에 유저가 선택한 카테고리를 삽입한다.
- 카테고리 선택을 취소하면, 해당 카테고리를 삭제한다.

- 버튼을 누르면 아래를 실행한다.

<실행>
- target 변수에 m의 요소를 펼쳐서 넣는다.
- Math.floor(Math.random()*(target.length))을 실행한다. (0 ~ 마지막index) 정수만나옴
- 해당 인덱스 정보를 유저한테 노출시킨다.

<한번 더 돌리기 전 입력창>
<1. 유저가 생각하는 메뉴 (패스가능)>
- .txt의 제목으로 DropDown을 만든다.
- (텍스트.trim()!=='')일 때만 실행
  - 

<2. 룰렛에 넣을 수 있는 메뉴인가요? O / X> (필수)
- 